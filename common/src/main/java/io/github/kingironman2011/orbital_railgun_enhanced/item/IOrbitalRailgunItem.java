package io.github.kingironman2011.orbital_railgun_enhanced.item;

/**
 * Marker interface implemented by every version-specific {@code OrbitalRailgunItem}.
 *
 * <p>Common code (e.g. mixins) that needs to identify the railgun item without
 * importing a version-specific class should use {@code instanceof IOrbitalRailgunItem}
 * instead of {@code instanceof OrbitalRailgunItem}.
 */
public interface IOrbitalRailgunItem {
}
